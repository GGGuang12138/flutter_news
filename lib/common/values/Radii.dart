import 'package:flutter/material.dart';

class Radii {
  static const BorderRadiusGeometry k6pxRadius = BorderRadius.all(Radius.circular(6));
  static const BorderRadiusGeometry k9pxRadius = BorderRadius.all(Radius.circular(20));
}
